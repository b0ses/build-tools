- import_playbook: setup_droplet.yml
- name: Configure droplet for Custum Stream Dashboard
  hosts: custom-stream-dashboard
  remote_user: root
  become_method: sudo

  vars:

  tasks:

    - name: apply apt-get update --fix-missing
      command: apt-get update --fix-missing

    - name: install java
      apt: pkg=default-jdk
